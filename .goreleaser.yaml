version: 2

project_name: kukatko

before:
  hooks:
    # Keep the module tidy and build the embedded SPA so the binary ships the
    # current frontend (go:embed captures internal/web/static/dist).
    - go mod tidy
    - sh -c "cd web && npm ci && npm run build"
    # goreleaser checks the git state before these hooks run, so whatever they
    # leave behind ships unnoticed — that is how every binary up to v0.8.0 was
    # stamped v0.8.0+dirty (the Vite build deleted the tracked
    # internal/web/static/dist/.gitkeep; web/build/gitkeep.ts restores it now).
    # Stop the release rather than publish a binary that misreports its origin.
    - ./scripts/assert-clean-tree.sh

builds:
  - id: kukatko
    main: ./cmd/kukatko
    binary: kukatko
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      # arm64 is the production target (Raspberry Pi); amd64 is for dev/CI.
      - arm64
      - amd64
    ldflags:
      - -s -w
      - -X github.com/panbotka/kukatko/internal/version.Version={{.Version}}
      - -X github.com/panbotka/kukatko/internal/version.Commit={{.ShortCommit}}

archives:
  - id: tarball
    formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    files:
      - README.md

nfpms:
  - id: deb
    package_name: kukatko
    vendor: Pan Botka
    homepage: https://github.com/panbotka/kukatko
    maintainer: Tomas Kozak <kozak@talko.cz>
    description: |
      Kukátko — a self-hosted photo and video library for a personal or family
      archive. Single Go binary with an embedded React web UI, backed by
      PostgreSQL + pgvector, with semantic and full-text search, face
      recognition, and an optional external embeddings service.
    license: MIT
    section: misc
    priority: optional
    bindir: /usr/bin
    formats:
      - deb
    file_name_template: '{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}'
    dependencies:
      # exiftool (metadata), heif-convert (HEIC), dcraw (RAW preview), ffmpeg
      # (video metadata probe + poster/thumbnails), psql (DB tooling).
      # No texlive — the photo book is out of scope.
      - libimage-exiftool-perl
      - libheif-examples | libheif-bin
      - dcraw
      - ffmpeg
      - postgresql-client
      - ca-certificates
    contents:
      - src: deb/kukatko.service
        dst: /lib/systemd/system/kukatko.service
      - src: deb/kukatko.env
        dst: /etc/kukatko/kukatko.env
        type: config|noreplace
    scripts:
      postinstall: deb/postinstall.sh
      preremove: deb/preremove.sh
      postremove: deb/postremove.sh

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'

release:
  footer: |
    ---
    Released by [GoReleaser](https://goreleaser.com).
