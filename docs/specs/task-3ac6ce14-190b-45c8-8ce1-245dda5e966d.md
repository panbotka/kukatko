# M7 — i18n completeness

Ensure full Czech/English coverage with Czech as default and no hardcoded user-facing strings.

## Context
Read `docs/ARCHITECTURE.md` §13 (two languages, cs default). i18next is set up. This pass closes
gaps accumulated across feature tasks.

## Requirements
- Sweep all components/pages for hardcoded user-facing text; move everything into i18next resource
  files. Ensure both `cs` and `en` have complete, parallel keys (no missing translations).
- Verify date/number formatting respects locale; pluralization handled where needed.
- Backend user-facing messages that reach the UI (errors) should be translatable on the client
  (use stable codes/keys rather than raw English where practical).
- Add a lightweight check (script or test) that fails if `cs` and `en` key sets diverge or keys
  are missing, to prevent future drift.

## Quality gate (mandatory)
- `make check` MUST pass (frontend ESLint + Vitest).
- Test: cs/en resource key sets are identical (no missing/extra keys); a representative sample of
  screens renders without missing-translation warnings; language switch updates all visible text.
- Typed components.