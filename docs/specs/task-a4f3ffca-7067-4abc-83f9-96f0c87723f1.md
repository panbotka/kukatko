# Places — UI

Browse the library by location: a Places page showing countries → cities with photo counts, drilling into a photo grid scoped to the selected place. Backend: `GET /api/v1/places` returns `{places:[{country,count,cities:[{city,count}]}]}` (optional `?country=` to drill into one country), and `GET /photos?country=&city=` returns photos for a place (honoring the usual filters/sort/paging).

## Requirements
- `web/src/services/places.ts`: `fetchPlaces(country?, signal)` → typed hierarchy; types `PlaceCountry` / `PlaceCity`.
- `PlacesPage` at `/places`: list countries with their photo counts; selecting a country reveals its cities (with counts); selecting a city shows the photo grid scoped to that place. Reuse `PhotoGrid` + `FilterBar`. Load the scoped photos with the existing paginated-photos hook approach (like `useScopedPhotos`, extended to accept `country`/`city`). Loading / empty / error states.
- Keep the place + drill state in the URL so Back works: e.g. `/places?country=…&city=…` via `useUrlState`/History API.
- Add a "Places" (cs: "Místa") nav link in `Layout` for all authenticated users.
- i18n cs + en in BOTH locale files (Czech default). No hardcoded UI text.
- Vitest test: rendering the country list, drilling into a country shows its cities, and selecting a city scopes the grid (calls the photos fetch with country/city).

## Done criteria
- Update `CLAUDE.md`/`README.md` frontend sections.
- `make check` must pass (ESLint strict + Prettier + Vitest). No `any` without reason.