Finish translating `docs/FRONTEND.md` from Czech to English. This continues the repo-wide "docs & comments must be English" cleanup (see CLAUDE.md § Language). README.md and most other docs are already done; this file is only partially translated — ~617 lines still contain Czech.

## Scope
- Translate ALL remaining Czech prose/headings in `docs/FRONTEND.md` to fluent, idiomatic English.
- Find remaining Czech with: `grep -nIE '[ěščřžůňďť]' docs/FRONTEND.md` (`rg` is NOT installed on this box — use `grep`). Also watch for diacritic-free Czech words that the pattern won't catch.

## Rules (do not break)
- Keep the product name **Kukátko** exactly as-is.
- Keep unchanged: code blocks, shell commands, env-var names (`KUKATKO_*`), URLs, file paths, identifiers, config keys, component/hook/page/service names, endpoint paths.
- Preserve the Markdown structure and the **existing hand-wrapping** (line lengths / where lines break). Edit prose in place; do **NOT** reflow/rewrap paragraphs and do **NOT** run prettier or any formatter on docs — these are hand-wrapped and are **not** part of the Prettier gate.
- When the doc quotes actual **Czech UI text** to describe the interface (button labels e.g. "Ano"/"Ne"/"Nevím", "Žebříček", menu items, page titles shown to users), **keep the quoted Czech string** and translate only the surrounding explanatory prose.
- Don't drop information or restructure beyond what translation requires.

## Definition of Done
`make check` must pass (docs-budget + fmt + lint + typecheck + tests + frontend — a docs-only change must not break any of these; if it does, you touched something you shouldn't have). Then commit (in English) and push. End the commit message with:
`Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`